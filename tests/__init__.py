"""Project-root integration tests directory.

The main pytest suite lives in `backend/tests/`. This directory is a place
for cross-stack / smoke tests that touch both the backend and frontend once
the Docker stack is up. Run via `make test` which delegates to backend tests.
"""
